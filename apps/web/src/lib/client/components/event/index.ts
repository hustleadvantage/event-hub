import { CheckoutButton, StickyCheckoutButton } from './checkout-button';
import { Header } from './header';
import { Location } from './location';
import { StructuredContent as Content } from './structured-content';

export const Event = {
	CheckoutButton,
	Content,
	Header,
	Location,
	StickyCheckoutButton
};
